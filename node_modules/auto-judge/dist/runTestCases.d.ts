import { TestCase } from './getBaekjoonTestCase';
export type timeout = 'timeout';
interface TestResult {
    number: number;
    isSuccess: boolean;
    time: string;
    expected: string;
    received: string | timeout;
}
export interface TotalResult {
    isAllSuccess: boolean;
    totalTime: string;
    testResults: TestResult[];
    numberOfTest: {
        total: number;
        passed: number;
        failed: number;
    };
}
declare const runTestCases: (testCases: TestCase[], executeCommand: string, specificCase: number | undefined) => Promise<TotalResult>;
export default runTestCases;
